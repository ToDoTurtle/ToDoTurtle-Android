package com.eps.todoturtle.aboutus.logic.routes

import com.eps.todoturtle.R

enum class AboutUsRoutes(val stringResId: Int) {
    IMAGE(R.string.aboutus_image_uri),
    TEXT(R.string.aboutus_uri),
    ;
}
